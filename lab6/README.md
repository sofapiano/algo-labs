# Лабораторная работа №15 — Семантический поиск

Эх друзья, наш совместный путь подходит к концу. И финал этого пути - эта лаба.
Вы когда-нибудь задумывались, почему поисковик понимает, что «купить кроссовки» и «приобрести кеды» — это одно и то же? Ответ: эмбеддинги. Всё довольно просто. Мы берем кусок текста, прогоняем его через некий black box (модель) и получаем некоторый вектор. Вектор отражает пространственную близость. Это кстати довольно фундаментально, есть [вероятностный подход](https://education.yandex.ru/handbook/ml/article/veroyatnostnyj-podhod-v-ml) к машинному обучению и [геометрический подход](https://naukamirowozreniya.ru/public/202511/application/1764226866949650581/geometricheskie-osnovy-mashinnogo-obucheniya.pdf) к машинному обучению. Вот как раз это явный пример геометрического определения, где данные - точки в пространстве.

---

### Что такое эмбеддинг

Эмбеддинг — это плотный числовой вектор фиксированной размерности (например, 384 числа типа `float32`), который языковая модель строит для текста. Ключевое свойство: семантически похожие тексты получают геометрически близкие векторы.

На подумать, а как бы вы обучили такую модель?

```
"кот сидит на подоконнике"   → [0.12, -0.34, 0.87, ...]   ┐
"кошка лежит на окне"        → [0.11, -0.31, 0.85, ...]   ┘ близко
"ракета летит на орбиту"     → [-0.72, 0.55, -0.10, ...]    далеко
```

Расстояние между векторами — мера семантической близости. Чаще всего используют косинусное сходство или евклидово расстояние (для нормализованных векторов они эквивалентны).

В этой лабораторной эмбеддинги считаются с помощью небольшой модели `all-MiniLM-L6-v2`, которая загружается через `sentence-transformers` прямо в процессе:

```python
from embedding import Embeddings

emb = Embeddings()                          # загружает all-MiniLM-L6-v2 при первом вызове
vec = emb.embed("кот сидит на подоконнике") # np.ndarray, shape (384,)
```

P.S. вам никто не мешает заменить потроха модуля embedding.py для того чтобы вызывать большую модель откуда угодно, это пример "из коробки"

---

### Цель работы

Построить структуру данных, которая индексирует текстовые эмбеддинги и позволяет находить `k` ближайших соседей за **O(k · log N)** вместо наивного **O(k · N)**.


**СТРУКТУРУ ДАННЫХ ПИШЕМ РУЧКАМИ, НИКАКИХ СТОРОННИХ БИБЛИОТЕК!**
---

## Задание

| Задача | Сложность |
|---|---|
| 1. Семантический индекс | Базовый |
| 2. Сравнение с линейным поиском | Базовый |
| 3. Streamlit-визуализация | Базовый |
| 4. Расширенная Streamlit-визуализация | Hard |
---

## Задача 1: Семантический индекс

### Постановка

Нужно построить структуру данных, которая:
- хранит набор пар `(вектор, payload)`;
- поддерживает вставку нового элемента;
- отвечает на запрос «найти `k` ближайших к данному вектору элементов».

Сложность поиска должна быть **O(k · log N)**, где N — количество элементов в индексе.

### Идея

Дерево. Конкретная реализация — на ваш выбор:

### Интерфейс

Реализуйте совместимый интерфейс:

```python
import numpy as np
from dataclasses import dataclass


@dataclass(frozen=True)
class Record:
    id: int
    text: str


@dataclass(frozen=True)
class SearchResult:
    id: str
    distance: float
    score: float
    path: tuple[str, ...]  # путь по узлам дерева от корня до листа
    payload: Record | None


class YourIndex:
    def insert(
        self,
        vector: np.ndarray,
        item_id: str,
        payload: Record | None = None,
    ) -> None:
        """Добавить вектор в индекс."""
        ...

    def search(
        self,
        query: np.ndarray,
        k: int = 5,
    ) -> list[SearchResult]:
        """Вернуть k ближайших к query элементов."""
        ...
```

### Пример работы

```
Индекс построен на N текстах.
Текста вам поможет сгенерировать LLM, данные нужно расширить, внеся мусор (символы вначале и в конце, пробелы и тд.)  
Базовый корпус будет приложен, но его **необходимо расширить хотя бы в два раза**  
Запрос: "космический аппарат и его траектория"

Результат:
  1. id=42  distance=0.182  "Орбитальный спутник меняет курс"
  2. id=17  distance=0.210  "Расчёт баллистической траектории ракеты"
  3. id=88  distance=0.239  "МКС выполняет манёвр уклонения"
  4. id=5   distance=0.301  "Двигатели космического корабля"
  5. id=61  distance=0.329  "Гравитационный манёвр у Юпитера"
```

---

## Задача 2: Сравнение с линейным поиском

### Постановка

Убедитесь экспериментально, что ваш индекс быстрее наивного перебора, и покажите, как растёт разрыв с увеличением N.

### Что нужно сделать

1. Реализовать **линейный поиск** — пройти по всем N векторам и выбрать k ближайших:

```python
def linear_search(
    vectors: np.ndarray,     # shape (N, dim)
    payloads: list[Record],
    query: np.ndarray,       # shape (dim,)
    k: int,
) -> list[SearchResult]:
    """Наивный O(k·N) поиск ближайших соседей."""
    ...
```

2. Заполнить индекс и линейный поиск одними и теми же данными для N ∈ {100, 500, 1000, 5000, 10000}.

3. Замерить время поиска k=10 ближайших соседей для каждого N.

4. Построить таблицу и/или график.

### Ожидаемый результат

```
N       Линейный (ms)   Дерево (ms)   Ускорение
------  -------------   -----------   ---------
100          0.8            1.2          0.7x   ← на маленьких N дерево медленнее
500          4.1            2.1          2.0x
1000         8.3            3.4          2.4x
5000        41.2           11.8          3.5x
10000       83.7           19.2          4.4x
```

> На маленьких N дерево проигрывает из-за накладных расходов — это нормально. Важна асимптотика.

Если теория не подтвердилась, опишите почему так   
---

## Задача 3 (hard): Streamlit-визуализация

### Постановка

Подключите вашу реализацию индекса к Streamlit-приложению: пользователь вводит запрос, видит результаты и структуру дерева.

## Пример интерфейса (ожидаемый результат работы)
<img width="1703" height="924" alt="Example" src="https://github.com/user-attachments/assets/14b6439a-0f2c-40c6-8bd0-c5e888d9d315" />

Чем круче и обширнее будет ваша визуализация, тем лучше!

### Что нужно сделать

1. Взять `st_demo.py` как отправную точку.
2. Заменить `SemanticBTree` на вашу реализацию — интерфейс `insert` / `search` должен совпадать (но если отличается, ничего страшного)  
3. Адаптировать визуализацию дерева под вашу структуру: **показать узлы, разбиения, путь до найденного листа**.

### Запуск

```bash
uv run streamlit run st_demo.py
```

### Что должно работать в UI

- **Поиск**: ввести текстовый запрос → top-k результатов с расстояниями и путём в дереве.
- **Добавление**: ввести текст → вставляется в индекс, дерево перерисовывается.
- **Удаление**: ввести id → запись удаляется, дерево перестраивается.
- **Визуализация**: дерево отображается в виде интерактивной схемы.

---

## Требования к отчёту

### 1. Реализация (задача 1)
- Описание выбранной структуры и обоснование выбора
- Анализ сложности: почему поиск O(k · log N)?
- Что происходит при высокой размерности — работает ли гарантия?

### 2. Бенчмарк (задача 2)
- Таблица с результатами замеров
- График «время vs N» для обоих методов
- При каком N дерево начинает выигрывать?

### 3. Визуализация (задача 3, если выполняли)
- Скриншоты или запись демо
- Что было сложнее всего адаптировать под свою структуру?

### 4. Выводы
Ваши мысли: что удивило, что не сработало так, как ожидалось, и почему «проклятие размерности» — это реальная проблема.

---

## Полезные ресурсы

1. [Ball Tree — sklearn](https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.BallTree.html)
2. [k-d tree — Wikipedia](https://en.wikipedia.org/wiki/K-d_tree)
3. [Sentence Transformers — модели и примеры](https://www.sbert.net/docs/sentence_transformer/pretrained_models.html)
4. [Faiss — промышленный vector search от Meta](https://github.com/facebookresearch/faiss)

---

```bash
# Проверка типов и стиля
make type-check
make lint
```


